{ "query": "query { organization ( login:\"%s\" ) { membersWithRole ( first:%d ) { pageInfo { hasNextPage endCursor } edges { hasTwoFactorEnabled role node { id login name email company url bio status { message } updatedAt repositoriesContributedTo ( first: 20, orderBy: { field: NAME, direction: ASC } ) { totalCount nodes { nameWithOwner } } } } totalCount } } }" }
