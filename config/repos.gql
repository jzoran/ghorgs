query {
  organization ( login: "SonyMobile" ) {
    repositories ( first: %d ) {
      pageInfo {
        hasNextPage
        endCursor
      }
      nodes {
        id
        name
        isPrivate
        url
        diskUsage
        updatedAt
        pushedAt
      }
      totalCount
    }
  }
}
