{ "query": "query { organization ( login: \"%s\" ) { repositories ( first: %d, after: \"%s\" ) { pageInfo { hasNextPage endCursor } nodes { id name isPrivate url diskUsage updatedAt pushedAt } totalCount } } }" }
